import React from 'react';

const NumberContext = React.createContext();
//khai báo 1 context
// 1  context sẽ có provider và consumer tương ứng 
// consumer nằm trong provider 

export default NumberContext;

// import to NumberProvider.js